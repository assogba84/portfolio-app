name: My first pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code 
        uses: actions/checkout@v4
      - name: build image
        run : docker build -t assogba84/ciporfolio .
      - name: List images
        run: docker images
      